# ==============================================================================
# i3wm Keybinding Modes
# Contiene definiciones para modos de i3, como el modo de redimensionar.
# ==============================================================================

# --- Modo de Redimensionar Ventanas ---
# Este modo se activa con $mod+r (ver 20-keybindings-base.conf)
mode "resize" {
        # Redimensionar con jklñ
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym ntilde resize grow width 10 px or 10 ppt

        # Alternativa con flechas
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # Volver al modo normal
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

# --- Otros modos (si los necesitas) ---
# Puedes añadir más modos aquí, por ejemplo:
# mode "mi_modo" {
#     bindsym ...
#     bindsym Return mode "default"
#     bindsym Escape mode "default"
# }
