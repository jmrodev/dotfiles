gupm() {
    if [[ -z "$1" ]]; then
        echo "Aviso: Debes especificar un nombre de rama."
        echo "Uso: gupm <rama>"
        return 1
    fi

    local branch_name="$1"
    echo "--- Actualizando desde 'origin/$branch_name' ---"
    git pull --rebase origin "$branch_name"
}