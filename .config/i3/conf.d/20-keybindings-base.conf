# ==============================================================================
# i3wm Keybindings (Base)
# Contiene atajos fundamentales de i3 y comandos de sistema.
# ==============================================================================

# --- Comandos Básicos de i3 ---
# Abrir una terminal (i3-sensible-terminal intentará usar xfce4-terminal en tu caso)
#bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec konsole

# Matar la ventana actual
bindsym $mod+Shift+q kill

# Recargar la configuración de i3
bindsym $mod+Shift+c reload
# Reiniciar i3 (completo)
bindsym $mod+Shift+r restart
# Salir de i3 (con confirmación)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m '¿Realmente quieres salir de i3? Esto terminará tu sesión X.' -B 'Sí, salir de i3' 'i3-msg exit'"

# --- Navegación y Movimiento de Ventanas ---
# Tienes dos conjuntos de atajos para left/down/up/right (jklñ y flechas).
# Puedes elegir uno o mantener ambos si te sientes cómodo.
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+ntilde focus right
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+ntilde move right
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# --- Diseño de Ventanas (Layout) y Floating ---
bindsym $mod+h split h
bindsym $mod+v split v
bindsym $mod+f fullscreen toggle
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split # ¡Atajo para layout toggle split!
bindsym $mod+Shift+space floating toggle
bindsym $mod+space focus mode_toggle
# Alternar entre tiled y floating focus
bindsym $mod+a focus parent
# Enfocarse en el contenedor padre

# --- Workspaces ---
# Aquí usas variables $ws1, $ws2, etc. (definidas en 00-variables.conf, ¡bien hecho!)
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# Renombrar workspace
bindsym $mod+t exec i3-input -F 'rename workspace to "%s"' -P 'Nuevo nombre para workspace: '

# --- Modo "sistema" (COMENTADO - USAMOS ROFI POWER MENU EN SU LUGAR) ---
# Si deseas volver a usar este modo, descomenta las líneas.
# mode "sistema" {
#     bindsym l exec --no-startup-id i3lock --color 000000; mode "default"
#     bindsym x exec --no-startup-id "i3-nagbar -t warning -m '¿Salir de i3?' -B 'Sí' 'i3-msg exit'"; mode "default"
#     bindsym s exec --no-startup-id "systemctl suspend && i3lock --color 000000"; mode "default"
#     bindsym h exec --no-startup-id "systemctl hibernate && i3lock --color 000000"; mode "default"
#     bindsym r exec --no-startup-id systemctl reboot; mode "default"
#     bindsym p exec --no-startup-id systemctl poweroff; mode "default"
#     bindsym Return mode "default"
#     bindsym Escape mode "default"
# }
# bindsym $mod+Pause mode "sistema"

# --- Script de Bindings Adicionales ---
bindsym $mod+F1 exec --no-startup-id $scriptsdir/enhanced_i3_bindings.sh
# Asegúrate de que este script no duplique atajos ya definidos aquí para evitar conflictos.
